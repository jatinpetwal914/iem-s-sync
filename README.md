# IEM Sync

Synchronized beat platform for music teams. The network distributes master timing. Each device generates its own click with the Web Audio API.

Phase 1 is the project foundation: Next.js, TypeScript, Tailwind, architecture layers, tempo math, genre config, and authorization contracts.

```bash
npm install
npm run dev
```
